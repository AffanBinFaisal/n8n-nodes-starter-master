import type { INodeProperties } from 'n8n-workflow';

export const loanGetLoansDescription: INodeProperties[] = [
	// Add operation-specific parameters here when needed (use displayOptions: { show: { operation: ['getLoans'], resource: ['loan'] } })
];
